# =============================================================================
# PHOENIX AIOS Python 依赖 — 生产环境
# =============================================================================
# 安装: pip install -r requirements.txt
# =============================================================================

# ── 核心依赖 ──────────────────────────────────────────────────────────────
# PHOENIX Core 使用 Python 标准库，无外部依赖
# 以下为可选增强依赖

# Web 框架（可选，用于增强 API）
# flask>=3.0.0
# fastapi>=0.109.0
# uvicorn>=0.27.0

# 数据库
# sqlalchemy>=2.0.25
# alembic>=1.13.1

# 缓存
redis>=5.0.1

# 任务队列
# celery>=5.3.6

# ── 监控依赖 ──────────────────────────────────────────────────────────────
# Sentry 错误监控
sentry-sdk>=1.39.2

# Prometheus 指标
prometheus-client>=0.19.0

# ── 工具依赖 ──────────────────────────────────────────────────────────────
# HTTP 客户端
requests>=2.31.0
httpx>=0.26.0

# JSON 处理
orjson>=3.9.10

# 日志
structlog>=24.1.0
python-json-logger>=2.0.7

# 日期时间处理
python-dateutil>=2.8.2

# 环境变量管理
python-dotenv>=1.0.0

# 配置管理
pydantic>=2.5.3
pydantic-settings>=2.1.0

# ── 安全依赖 ──────────────────────────────────────────────────────────────
# JWT 认证
PyJWT>=2.8.0

# 密码哈希
bcrypt>=4.1.2

# 加密
cryptography>=41.0.7

# ── 开发依赖 ──────────────────────────────────────────────────────────────
# 测试
pytest>=7.4.4
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
pytest-timeout>=2.2.0
pytest-asyncio>=0.23.3

# 代码质量
ruff>=0.1.9
black>=24.1.1
isort>=5.13.2
mypy>=1.8.0

# 安全扫描
safety>=2.3.5
bandit>=1.7.7

# 文档
sphinx>=7.2.6
sphinx-rtd-theme>=2.0.0
